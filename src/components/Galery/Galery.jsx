import classes from './Galery.module.scss';
import Selector from '../Selector/Selector';

const Galery = () => {
    return (
        <Selector title={'Galeria'}>
            <h4>Proximamente</h4>
        </Selector>
    );
};

export default Galery;